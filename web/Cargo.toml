[package]
name = "web"
version.workspace = true
edition.workspace = true

[dependencies]
# No `ssr` feature: that gates the sqlx derives, which don't build for wasm.
shared = { path = "../shared" }

leptos = { version = "0.8", features = ["csr"] }
leptos_router = { version = "0.8" }
gloo-net = { version = "0.7", features = ["http"] }
serde = { workspace = true }
serde_json = { workspace = true }
console_error_panic_hook = "0.1"
# leptos pulls web-sys in already; this only turns on the Storage feature,
# which the dashboard needs to remember the selected category tab.
web-sys = { version = "0.3", features = ["Storage"] }
